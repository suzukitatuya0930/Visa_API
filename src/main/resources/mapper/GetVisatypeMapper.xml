<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.GetVisatypeMapper">

    <select id="getVisatype" resultType="com.example.demo.model.GetVisatypeModel" >
   		SELECT  visatype_id,visatype_name FROM visatype_master WHERE delete_flag = 0;
    </select>

</mapper>

