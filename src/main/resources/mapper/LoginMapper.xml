<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.LoginMapper">

    <select id="login" resultType="com.example.demo.model.LoginModel" >
   		SELECT email,password,role_name
		FROM visa_login
		INNER JOIN role_master 
		on visa_login.role_id = role_master.role_id
		WHERE email = #{email} AND password = #{password} AND 	 visa_login.delete_flag = 0
	
    </select>
    

</mapper>

